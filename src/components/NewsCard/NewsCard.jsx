import React from 'react';
import './NewsCard.css';  // We'll need to create this CSS file later

function NewsCard({ article }) {
  return (
    <div className="news-card">
      {/* We'll add the content here */}
    </div>
  );
}

export default NewsCard;